import "bootcss";
import "jquery";
import 'bootjs';
import "../css/style.css";
import './../css/register.css';

function changePage(params) {
    if (params == '0') {
        
    } else {
        
    }
}